[project]
name = "ms-metadata-catalog-service"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi[standard]>=0.128.8",
    "pymongo>=4.9.0",
    "pydantic-settings>=2.7.0",
    "pydantic>=2.13.5",
]

[dependency-groups]
dev = [
    "httpx>=0.28.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
]
